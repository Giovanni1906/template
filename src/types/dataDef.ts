// Define el tipo de datos esperado para cada elemento del arreglo
interface userData {
    id: number;
    name: string;
    // Otros campos si los hay
  }

  interface todosData {
    id: number;
    title: string;
    // Otros campos si los hay
  }

  interface HomepageMeta {
    title: string;
    description: string;
    // Añade otros campos según sea necesario
  }
  